## Трекер задач сотрудников

### Инициализация проекта и базовая конфигурация
Что сделано:
- Инициализирован Django-проект и создано приложение `tracker`
- Подключён `Django REST Framework`
- Настройки проекта вынесены в переменные окружения `(.env)`
- Добавлена базовая конфигурация `settings.py`:
    - `DEBUG`, `SECRET_KEY`, `ALLOWED_HOSTS` через `env` 
    - единый тип первичного ключа `BigAutoField` (более безопасный стандарт)
- Реализован тестовый API-эндпоинт для проверки работоспособности сервера (`/api/health/`)
- Добавлен `.env.example`

Подготовлена структура проекта для дальнейшего расширения